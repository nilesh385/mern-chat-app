import React from "react";
import { Card } from "../ui/card";

type Props = {};

const Conversations = (props: Props) => {
  return <Card className="h-full flex">Conversations</Card>;
};

export default Conversations;
